{
  "name": "myunihub-playwright",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "npx playwright install-deps && sleep 10 && npx playwright install chromium && node index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.43.1"
  }
}
